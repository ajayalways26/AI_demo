CREATE OR REPLACE TABLE adam_data_engg_fsi_demo.EDW_MART.DIM_ACCOUNT (
    ACCOUNT_ID VARCHAR(64) COMMENT 'Unique identifier for the account',
    ACCOUNT_NAME VARCHAR(128) COMMENT 'Name of the account',
    ACCOUNT_NUMBER VARCHAR(64) COMMENT 'Account number associated with the account',
    ACCOUNT_TYPE_ID BIGINT COMMENT 'Identifier for the account type',
    BUSINESS_COLUMN VARCHAR(64) COMMENT 'Business-specific column used for categorization',
    CLOSE_DATE DATE COMMENT 'Date when the account was closed',
    CREATED_BY VARCHAR(64) COMMENT 'User who created the account record',
    CREATE_BATCH_ID BIGINT COMMENT 'Batch ID associated with when the record was created',
    CREATE_DATE_TIME TIMESTAMP_NTZ COMMENT 'Timestamp indicating record creation',
    CURRENCY_CODE VARCHAR(64) COMMENT 'Currency code associated with the account',
    CURRENT_FLAG CHAR(1) COMMENT 'Flag indicating if the record is current (Y/N)',
    CUSTOMER_ID BIGINT COMMENT 'Identifier for the associated customer',
    DATA_SOURCE VARCHAR(64) COMMENT 'Source system of the data',
    EFFECTIVE_FROM TIMESTAMP_NTZ NOT NULL COMMENT 'Timestamp indicating when the record becomes effective',
    EFFECTIVE_TO TIMESTAMP_NTZ COMMENT 'Timestamp indicating when the record ceases to be effective',
    IS_DELETED CHAR(1) COMMENT 'Flag indicating if the record is deleted (Y/N)',
    OPEN_DATE DATE COMMENT 'Date when the account was opened',
    PRODUCT_ID BIGINT COMMENT 'Identifier for the associated product',
    SK_ACCOUNT_KEY BIGINT NOT NULL COMMENT 'Surrogate key for the account',
    SK_ACCOUNT_STATUS_KEY BIGINT COMMENT 'Surrogate key for the account status',
    SK_ACCOUNT_TYPE_KEY BIGINT COMMENT 'Surrogate key for the account type',
    SK_CUSTOMER_KEY BIGINT COMMENT 'Surrogate key for the customer',
    SK_PRODUCT_KEY BIGINT COMMENT 'Surrogate key for the product',
    STATUS_ID BIGINT COMMENT 'Identifier for the account status',
    UPDATED_BY VARCHAR(64) COMMENT 'User who last updated the record',
    UPDATE_BATCH_ID BIGINT COMMENT 'Batch ID associated with when the record was last updated',
    UPDATE_DATE_TIME TIMESTAMP_NTZ COMMENT 'Timestamp indicating last record update',

    CONSTRAINT PK_DIM_ACCOUNT PRIMARY KEY (SK_ACCOUNT_KEY)
)