CREATE OR REPLACE TABLE adam_data_engg_fsi_demo.EDW_MART.DIM_ACCOUNT (
    ACCOUNT_ID VARCHAR(64) COMMENT 'The unique identifier for the account.',
    ACCOUNT_NAME VARCHAR(128) COMMENT 'The name of the account.',
    ACCOUNT_NUMBER VARCHAR(64) COMMENT 'The account number.',
    ACCOUNT_TYPE_ID BIGINT COMMENT 'Foreign key identifying the account type.',
    BUSINESS_COLUMN VARCHAR(64) COMMENT 'Business-specific column descriptor.',
    CLOSE_DATE DATE COMMENT 'The date when the account was closed.',
    CREATED_BY VARCHAR(64) COMMENT 'The user who created the record.',
    CREATE_BATCH_ID BIGINT COMMENT 'Batch identifier for record creation.',
    CREATE_DATE_TIME TIMESTAMP_NTZ COMMENT 'Timestamp of record creation.',
    CURRENCY_CODE VARCHAR(64) COMMENT 'The currency code associated with the account.',
    CURRENT_FLAG CHAR(1) COMMENT 'Indicates whether the account record is current.',
    CUSTOMER_ID BIGINT COMMENT 'Foreign key identifying the customer.',
    DATA_SOURCE VARCHAR(64) COMMENT 'The source system of data.',
    EFFECTIVE_FROM TIMESTAMP_NTZ NOT NULL COMMENT 'Effective start timestamp for the account.',
    EFFECTIVE_TO TIMESTAMP_NTZ COMMENT 'Effective end timestamp for the account, if applicable.',
    IS_DELETED CHAR(1) COMMENT 'Indicates if the record is marked as deleted.',
    OPEN_DATE DATE COMMENT 'The date when the account was opened.',
    PRODUCT_ID BIGINT COMMENT 'Foreign key identifying the product.',
    SK_ACCOUNT_KEY BIGINT NOT NULL COMMENT 'Surrogate key for the account.',
    SK_ACCOUNT_STATUS_KEY BIGINT COMMENT 'Surrogate key for account status.',
    SK_ACCOUNT_TYPE_KEY BIGINT COMMENT 'Surrogate key for account type.',
    SK_CUSTOMER_KEY BIGINT COMMENT 'Surrogate key for customer association.',
    SK_PRODUCT_KEY BIGINT COMMENT 'Surrogate key for product association.',
    STATUS_ID BIGINT COMMENT 'Foreign key identifying the status of the account.',
    UPDATED_BY VARCHAR(64) COMMENT 'The user who last updated the record.',
    UPDATE_BATCH_ID BIGINT COMMENT 'Batch identifier for record updates.',
    UPDATE_DATE_TIME TIMESTAMP_NTZ COMMENT 'Timestamp of the last record update.',

    CONSTRAINT PK_DIM_ACCOUNT PRIMARY KEY (SK_ACCOUNT_KEY)
) COMMENT = 'Dimension table for accounts with associated metadata and relationships.'