CREATE OR REPLACE TABLE adam_data_engg_fsi_demo.EDW_MART.DIM_ACCOUNT (
    ACCOUNT_ID         STRING COMMENT 'VARCHAR(64) in DB2',
    ACCOUNT_NAME       STRING COMMENT 'VARCHAR(128) in DB2',
    ACCOUNT_NUMBER     STRING COMMENT 'VARCHAR(64) in DB2',
    ACCOUNT_TYPE_ID    NUMBER(38,0) COMMENT 'BIGINT in DB2',
    BUSINESS_COLUMN    STRING COMMENT 'VARCHAR(64) in DB2',
    CLOSE_DATE         DATE COMMENT 'DATE in DB2',
    CREATED_BY         STRING COMMENT 'VARCHAR(64) in DB2',
    CREATE_BATCH_ID    NUMBER(38,0) COMMENT 'BIGINT in DB2',
    CREATE_DATE_TIME   TIMESTAMP_NTZ COMMENT 'TIMESTAMP in DB2',
    CURRENCY_CODE      STRING COMMENT 'VARCHAR(64) in DB2',
    CURRENT_FLAG       STRING COMMENT 'CHAR(1) in DB2',
    CUSTOMER_ID        NUMBER(38,0) COMMENT 'BIGINT in DB2',
    DATA_SOURCE        STRING COMMENT 'VARCHAR(64) in DB2',
    EFFECTIVE_FROM     TIMESTAMP_NTZ NOT NULL COMMENT 'TIMESTAMP in DB2',
    EFFECTIVE_TO       TIMESTAMP_NTZ COMMENT 'TIMESTAMP in DB2',
    IS_DELETED         STRING COMMENT 'CHAR(1) in DB2',
    OPEN_DATE          DATE COMMENT 'DATE in DB2',
    PRODUCT_ID         NUMBER(38,0) COMMENT 'BIGINT in DB2',
    SK_ACCOUNT_KEY     NUMBER(38,0) NOT NULL COMMENT 'BIGINT in DB2',
    SK_ACCOUNT_STATUS_KEY NUMBER(38,0) COMMENT 'BIGINT in DB2',
    SK_ACCOUNT_TYPE_KEY   NUMBER(38,0) COMMENT 'BIGINT in DB2',
    SK_CUSTOMER_KEY      NUMBER(38,0) COMMENT 'BIGINT in DB2',
    SK_PRODUCT_KEY       NUMBER(38,0) COMMENT 'BIGINT in DB2',
    STATUS_ID            NUMBER(38,0) COMMENT 'BIGINT in DB2',
    UPDATED_BY           STRING COMMENT 'VARCHAR(64) in DB2',
    UPDATE_BATCH_ID      NUMBER(38,0) COMMENT 'BIGINT in DB2',
    UPDATE_DATE_TIME     TIMESTAMP_NTZ COMMENT 'TIMESTAMP in DB2',

    PRIMARY KEY (SK_ACCOUNT_KEY)
);