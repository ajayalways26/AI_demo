CREATE OR REPLACE TABLE adam_data_engg_fsi_demo.EDW_MART.DIM_ACCOUNT (
    ACCOUNT_ID VARCHAR(64) COMMENT 'Unique identifier for the account',
    ACCOUNT_NAME VARCHAR(255) COMMENT 'Name of the account',
    ACCOUNT_NUMBER VARCHAR(64) COMMENT 'Account number',
    ACCOUNT_TYPE_ID BIGINT COMMENT 'Foreign key for account type',
    BUSINESS_COLUMN VARCHAR(64) COMMENT 'Business-specific column',
    CLOSE_DATE DATE COMMENT 'The closing date of the account',
    CREATED_BY VARCHAR(64) COMMENT 'User who created the record',
    CREATE_BATCH_ID BIGINT COMMENT 'Batch ID associated with creation',
    CREATE_DATE_TIME TIMESTAMP_NTZ COMMENT 'Timestamp when record was created',
    CURRENCY_CODE VARCHAR(64) COMMENT 'Currency code for transactions',
    CURRENT_FLAG CHAR(1) COMMENT 'Flag indicating the current record',
    CUSTOMER_ID BIGINT COMMENT 'Foreign key for the customer',
    DATA_SOURCE VARCHAR(64) COMMENT 'Source system of the data',
    EFFECTIVE_FROM TIMESTAMP_NTZ NOT NULL COMMENT 'Start of the effective period',
    EFFECTIVE_TO TIMESTAMP_NTZ COMMENT 'End of the effective period',
    IS_DELETED CHAR(1) COMMENT 'Flag indicating if the record is deleted',
    OPEN_DATE DATE COMMENT 'Date when the account was opened',
    PRODUCT_ID BIGINT COMMENT 'Foreign key for the product',
    SK_ACCOUNT_KEY BIGINT NOT NULL COMMENT 'Surrogate key for the account table',
    SK_ACCOUNT_STATUS_KEY BIGINT COMMENT 'Surrogate key for the account status',
    SK_ACCOUNT_TYPE_KEY BIGINT COMMENT 'Surrogate key for the account type',
    SK_CUSTOMER_KEY BIGINT COMMENT 'Surrogate key for the customer',
    SK_PRODUCT_KEY BIGINT COMMENT 'Surrogate key for the product',
    STATUS_ID BIGINT COMMENT 'Foreign key for the account status',
    UPDATED_BY VARCHAR(64) COMMENT 'User who last updated the record',
    UPDATE_BATCH_ID BIGINT COMMENT 'Batch ID associated with update',
    UPDATE_DATE_TIME TIMESTAMP_NTZ COMMENT 'Timestamp when record was last updated',
    CONSTRAINT PK_DIM_ACCOUNT PRIMARY KEY (SK_ACCOUNT_KEY)
)
COMMENT = 'DIM_ACCOUNT table migrated from DB2 to Snowflake';